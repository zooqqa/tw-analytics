# Changelog

Все значимые изменения в проекте будут документированы в этом файле.

## [MVP v1.0] - 2024-10-17

### ✅ Реализовано

#### Загрузка данных
- CLI команда для загрузки CSV из TrafficWave и Moloco
- Валидация и нормализация данных
- Хранение истории загрузок в БД
- Сохранение всех полей с префиксами `tw_` и `m_`

#### Витрина данных
- Объединение данных по ключу (дата, кампания)
- Расчёт метрик: `profit`, `roi_pct`, `calculated_spend`, `calculated_cpi`
- Корректная агрегация процентных метрик
- Приоритет `m_spend` над `calculated_spend` при наличии

#### UI
- Два режима таблицы:
  - Иерархическая (кампании → офферы) - по умолчанию
  - Плоская (все строки)
- Модальное окно фильтров:
  - Текстовые фильтры: "содержит" / "не содержит"
  - Числовые фильтры: "больше" / "меньше"
  - Диапазон дат
  - Фильтр по гео
- Независимость фильтров и отображаемых колонок
- Сортировка по любой колонке в обоих режимах
- Выбор видимых колонок через чекбоксы
- Агрегаты (карточки со статистикой)
- Dark theme UI

#### API
- `GET /` - главная страница
- `GET /table` - плоская таблица с фильтрами
- `GET /table/hierarchical` - иерархическая структура
- `GET /stats` - агрегированная статистика
- `GET /loads` - история загрузок

#### Технические детали
- FastAPI для REST API
- SQLAlchemy ORM
- PostgreSQL для хранения данных
- Pandas для обработки CSV
- Vanilla JavaScript для фронтенда

### 🐛 Исправленные баги

#### 2024-10-17 - Сортировка в иерархической таблице
- **Проблема**: Пропала возможность сортировки при клике на заголовок колонки
- **Решение**: Добавлена логика сортировки в `renderHierarchicalTable()`:
  - Добавлены `data-key` атрибуты к заголовкам
  - Добавлены обработчики клика для переключения сортировки
  - Добавлена сортировка массива кампаний перед рендерингом
  - Добавлены индикаторы направления сортировки (▲/▼)

#### 2024-10-17 - Moloco статистика показывала нули
- **Проблема**: Поля `m_impression`, `m_click`, `m_install`, `m_spend` были пустыми
- **Решение**: Исправлена логика в `merge_service.py`:
  - Создается словарь `moloco_map` по ключу `(дата, кампания)`
  - Для каждой строки TW добавляются соответствующие данные Moloco
  - Если данных нет - заполняются нули

#### 2024-10-17 - Иерархическая таблица по умолчанию
- **Проблема**: По умолчанию открывалась плоская таблица
- **Решение**: Изменён порядок табов и начальное значение `currentTab = 'hierarchical'`

### 📝 Документация
- Создан README.md с инструкциями по запуску
- Обновлён info.md с текущим статусом проекта
- Создан CHANGELOG.md для отслеживания изменений
- docs/mvp_plan.md обновлён с критериями готовности
- docs/features_plan.md содержит отложенные функции

### 🔄 Отложено на будущее
См. `docs/features_plan.md` (Этап 0):
- Инкрементальная загрузка вместо полного удаления
- UI для просмотра истории загрузок
- Сохранение и загрузка пресетов фильтров
- Экспорт данных (CSV/Excel/JSON)
- Индикация загрузки в UI
- Unit и Integration тесты
- Обработка ошибок в UI (toast-уведомления)

---

## Формат версионирования

Формат: `[тип] - YYYY-MM-DD`

### Типы изменений:
- ✅ **Реализовано** - новые функции
- 🐛 **Исправлено** - исправление багов
- 📝 **Документация** - изменения в документации
- 🔄 **Изменено** - изменения в существующих функциях
- ⚠️ **Устарело** - функции, которые будут удалены
- 🗑️ **Удалено** - удалённые функции
- 🔒 **Безопасность** - исправления уязвимостей

