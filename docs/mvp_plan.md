# План разработки MVP - Сервис сквозной аналитики

## Обзор проекта

Создание MVP сервиса для объединения и анализа данных из TrafficWave (трекер) и Moloco (рекламный источник) с возможностью фильтрации и анализа эффективности кампаний и офферов.

## Цели MVP

- Объединить данные из двух источников с сохранением всех полей и префиксов (`tw_`, `m_`)
- Создать удобный интерфейс для медиабайеров с полнотекстовой фильтрацией
- Обеспечить прозрачную загрузку CSV с историей и валидацией
- Показать ключевые метрики эффективности по согласованным формулам
- Сформировать базу для дальнейшего расширения (офферы, автоматизация, финансы)

## Этап 1: Базовая инфраструктура (1-2 недели)

### 1.1 Хранилище данных и схема БД

**Структура слоёв:**
- `raw` — хранение оригинальных CSV (S3/MinIO или локально `data/raw/<source>/<yyyy-mm-dd>/` + таблицы `raw_tw_events`, `raw_moloco_events` для журналирования); 
- `staging` — промежуточные таблицы с приведёнными типами и базовой валидацией (`stg_tw_events`, `stg_moloco_events`);
- `core` — нормализованные таблицы `tw_events`, `moloco_events` с префиксами;
- `mart` — витрина `merged_campaigns` для UI.

```sql
-- История загрузок
CREATE TABLE data_loads (
    id UUID PRIMARY KEY,
    source VARCHAR(50) NOT NULL,
    file_name VARCHAR(255) NOT NULL,
    file_hash CHAR(64) NOT NULL,
    records_total INTEGER,
    records_valid INTEGER,
    status VARCHAR(50) NOT NULL,
    error_log TEXT,
    started_at TIMESTAMP DEFAULT NOW(),
    finished_at TIMESTAMP
);

-- Сырые записи (опционально)
CREATE TABLE raw_tw_events (
    id SERIAL PRIMARY KEY,
    load_id UUID REFERENCES data_loads(id),
    payload JSONB,
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE raw_moloco_events (
    id SERIAL PRIMARY KEY,
    load_id UUID REFERENCES data_loads(id),
    payload JSONB,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Нормализованные таблицы
CREATE TABLE tw_events (
    id SERIAL PRIMARY KEY,
    load_id UUID REFERENCES data_loads(id),
    source_file VARCHAR(255),
    tw_date DATE,
    tw_link_title VARCHAR(500),
    tw_ad_campaign_name VARCHAR(500),
    tw_geo_country_code VARCHAR(10),
    tw_carrot_id VARCHAR(100),
    tw_first_purchases INTEGER,
    tw_registrations INTEGER,
    tw_installations INTEGER,
    tw_install2reg DECIMAL(5,2),
    tw_average_bill DECIMAL(10,2),
    tw_reg2dep DECIMAL(5,2),
    tw_purchases_sum DECIMAL(12,2),
    tw_epc DECIMAL(10,2),
    tw_income_usd DECIMAL(12,2),
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE moloco_events (
    id SERIAL PRIMARY KEY,
    load_id UUID REFERENCES data_loads(id),
    source_file VARCHAR(255),
    m_date DATE,
    m_campaign VARCHAR(500),
    m_campaign_id VARCHAR(100),
    m_app VARCHAR(200),
    m_app_id VARCHAR(100),
    m_creative VARCHAR(500),
    m_creative_id VARCHAR(100),
    m_impression INTEGER,
    m_click INTEGER,
    m_install INTEGER,
    m_action INTEGER,
    m_conversion INTEGER,
    m_spend DECIMAL(12,2),
    m_cpi DECIMAL(10,2),
    m_cpa DECIMAL(10,2),
    m_cost_per_conversion DECIMAL(10,2),
    m_currency VARCHAR(10),
    m_first_purchase INTEGER,
    m_registration INTEGER,
    m_purchase INTEGER,
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE merged_campaigns (
    id SERIAL PRIMARY KEY,
    load_id UUID REFERENCES data_loads(id),
    campaign_name VARCHAR(500),
    link_title VARCHAR(500),
    geo VARCHAR(10),
    date DATE,
    -- TW метрики
    tw_first_purchases INTEGER,
    tw_registrations INTEGER,
    tw_installations INTEGER,
    tw_install2reg DECIMAL(5,2),
    tw_reg2dep DECIMAL(5,2),
    tw_income_usd DECIMAL(12,2),
    -- Moloco метрики
    m_impression INTEGER,
    m_click INTEGER,
    m_install INTEGER,
    m_spend DECIMAL(12,2),
    m_cpi DECIMAL(10,2),
    m_first_purchase INTEGER,
    m_registration INTEGER,
    -- Расчётные поля
    cost DECIMAL(12,2),
    revenue DECIMAL(12,2),
    profit DECIMAL(12,2),
    roi_pct DECIMAL(8,2),
    install2reg_pct DECIMAL(8,2),
    reg2dep_pct DECIMAL(8,2),
    cpi_avg DECIMAL(10,2),
    cpa_avg DECIMAL(10,2),
    epc_avg DECIMAL(10,2),
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_tw_events_date_campaign ON tw_events (tw_date, tw_ad_campaign_name);
CREATE INDEX idx_moloco_events_date_campaign ON moloco_events (m_date, m_campaign);
CREATE INDEX idx_merged_campaigns_date_campaign ON merged_campaigns (date, campaign_name);
CREATE INDEX idx_data_loads_source_date ON data_loads (source, started_at);
```

**Дополнительно:**
- конвертация валют: даже если сейчас USD, закладываем таблицу `fx_rates(date, currency, rate_to_usd)`;
- хранение часовых поясов: `tw_timezone`, `m_timezone` (для отчетов «как в источнике»);
- обязательная миграционная стратегия (`Alembic`).

### 1.2 ETL-процесс

**Скрипты/команды:**
- `etl/load_tw_data.py` — загрузка CSV из TrafficWave;
- `etl/load_moloco_data.py` — загрузка CSV из Moloco;
- `etl/validate_data.py` — тесты качества (формат дат, отрицательные значения, пустые ключи, дубли);
- `etl/merge_data.py` — объединение в `merged_campaigns`;
- `etl/calculate_metrics.py` — расчет KPI/ROI с едиными формулами;
- `etl/rebuild_vitrine.py` — пересборка витрины по диапазону дат (в случае повторной загрузки);
- `etl/cleanup.py` — очистка временных файлов.

**Пошаговый процесс:**
1. Пользователь загружает CSV через UI или CLI, указывая источник (`TW`/`Moloco`).
2. Файл сохраняется в `data/raw` (или S3) + регистрируется запись в `data_loads` (`status = pending`).
3. Запускается загрузчик: построчно читает CSV, проводит первичную проверку (наличие обязательных колонок, валидные даты/числа, строки без `link_title`).
4. Неуспешные строки пишутся в лог и CSV ошибок (`data/errors/<load_id>.csv`), счётчик валидных/ошибочных строк фиксируется.
5. Валидные строки приводятся к типам и вставляются в `tw_events`/`moloco_events` с `load_id`.
6. Вызов `validate_data.py` для агрегированных проверок (дубли по `campaign+date`, отрицательные значения, расхождение валют).
7. `merge_data.py` агрегирует данные по `campaign`, `link_title`, `geo`, `date`, пересчитывает метрики и обновляет `merged_campaigns` (старые записи за период помечаются и заменяются).
8. `calculate_metrics.py` гарантирует единые формулы (см. раздел 2.1) и заполнение NULL вместо деления на ноль.
9. Статус `data_loads` обновляется на `completed` или `failed`, сохраняется путь к лог-файлу.
10. В UI отображается история загрузок (файл, дата, статус, строк обработано, ошибок, ссылка на лог).

**Ручная перезагрузка и откат:**
- повторная загрузка того же диапазона дат создаёт новый `load_id`, автоматически переагрегирует витрину;
- возможность отката к предыдущему `load_id` (флаг `active` в `merged_campaigns`);
- хранение checksum позволяет игнорировать идентичные файлы.

### 1.3 API и сервисы

**Эндпоинты FastAPI:**
- `POST /api/uploads` — загрузка CSV, ответ содержит `load_id` и ссылку на статус;
- `GET /api/uploads` — история загрузок (с пагинацией, фильтром по статусу и источнику);
- `GET /api/data` — получение данных с фильтрами (см. раздел 2.2);
- `GET /api/data/export` — выдача текущей выборки в JSON (для внешних BI);
- `GET /api/stats` — агрегированная статистика (общий spend/revenue/profit, количество кампаний, последняя загрузка);
- `GET /api/campaigns`, `GET /api/offers`, `GET /api/geos` — справочники для фильтров;
- `GET /api/alerts` — активные алерты;
- `GET /api/health` — проверка состояния ETL/БД.

**Фоновые задачи:**
- расписание (APScheduler/Celery beat) для проверки новых файлов в `data/inbox`;
- регулярная валидация (например, переагрегация за последние 7 дней для ловли поздних событий);
- очистка старых логов и временных файлов.

## Этап 2: Модуль медиабаинга (2-3 недели)

### 2.1 Дашборд кампаний

**Основная таблица (Tabulator/AG Grid):**
- Показывает все поля с префиксами `tw_`, `m_`, а также расчётные колонки.
- Поддерживает виртуализацию, заморозку столбцов, поиск по полям.
- Группировки: `campaign_name` → `link_title` → `geo`.
- Итоговые строки по каждой группе и по всей выборке.
- Возможность переключать набор колонок (предустановки «Основные», «TW», «Moloco»).

**Ключевые формулы:**
- `cost = m_spend`
- `revenue = COALESCE(tw_income_usd, 0)`
- `profit = revenue - cost`
- `roi_pct = CASE WHEN cost > 0 THEN (profit / cost) * 100 ELSE NULL END`
- `install2reg_pct = CASE WHEN tw_installations > 0 THEN (tw_registrations / tw_installations) * 100 ELSE NULL END`
- `reg2dep_pct = CASE WHEN tw_registrations > 0 THEN (tw_first_purchases / tw_registrations) * 100 ELSE NULL END`
- `cpi_avg = CASE WHEN tw_installations > 0 THEN cost / tw_installations ELSE NULL END`
- `cpa_avg = CASE WHEN tw_first_purchases > 0 THEN cost / tw_first_purchases ELSE NULL END`
- `epc_avg = CASE WHEN m_click > 0 THEN revenue / m_click ELSE NULL END`

**Агрегация при группировке:**
- суммируем `cost`, `revenue`, `profit`, `tw_installations`, `tw_registrations`, `tw_first_purchases`, `m_click`;
- пересчитываем процентные метрики на основе агрегированных сумм (а не среднее от средних);
- отображаем `—` в UI, если значение невозможно посчитать.

### 2.2 Система фильтрации

**UX:** одна кнопка «Фильтр» открывает попап с секциями; настройки сохраняются локально и могут быть экспортированы/импортированы.

```html
<button id="filterBtn" class="filter-button">🔍 Фильтр</button>

<dialog id="filterPopup" class="filter-popup">
  <form method="dialog">
    <section class="filter-section">
      <h3>Кампания</h3>
      <select name="campaign_mode">
        <option value="contains">Содержит</option>
        <option value="not_contains">Не содержит</option>
      </select>
      <textarea name="campaign_values" placeholder="camp1, camp2" rows="2"></textarea>
      <small>Перечисляйте через запятую, регистр игнорируется</small>
    </section>

    <section class="filter-section">
      <h3>Оффер</h3>
      <select name="offer_mode">
        <option value="contains">Содержит</option>
        <option value="not_contains">Не содержит</option>
      </select>
      <textarea name="offer_values" placeholder="offer1, offer2" rows="2"></textarea>
    </section>

    <section class="filter-section">
      <h3>Гео</h3>
      <select name="geo_mode">
        <option value="equals">Равно</option>
        <option value="not_equals">Не равно</option>
      </select>
      <input type="text" name="geo_values" placeholder="DE, FR">
    </section>

    <section class="filter-section">
      <h3>Диапазон дат</h3>
      <input type="date" name="date_from">
      <input type="date" name="date_to">
    </section>

    <section class="filter-section">
      <h3>Числовые метрики</h3>
      <div>
        <label>ROI %</label>
        <input type="number" name="roi_min" placeholder="≥" step="0.01">
        <input type="number" name="roi_max" placeholder="≤" step="0.01">
      </div>
      <div>
        <label>Profit $</label>
        <input type="number" name="profit_min" placeholder="≥" step="0.01">
        <input type="number" name="profit_max" placeholder="≤" step="0.01">
      </div>
      <div>
        <label>Spend $</label>
        <input type="number" name="spend_min" placeholder="≥" step="0.01">
        <input type="number" name="spend_max" placeholder="≤" step="0.01">
      </div>
      <div>
        <label>Installations</label>
        <input type="number" name="installs_min" placeholder="≥">
        <input type="number" name="installs_max" placeholder="≤">
      </div>
    </section>

    <section class="filter-section">
      <h3>Дополнительно</h3>
      <label><input type="checkbox" name="hide_zero_cost"> Скрыть строки с нулевыми тратами</label>
      <label><input type="checkbox" name="hide_zero_revenue"> Скрыть строки с нулевым доходом</label>
      <label><input type="checkbox" name="only_positive_roi"> Только ROI > 0</label>
    </section>

    <menu>
      <button value="cancel">Отмена</button>
      <button value="clear">Очистить</button>
      <button id="saveFilter">Сохранить пресет</button>
      <button value="default" id="applyFilter">Применить</button>
    </menu>
  </form>
</dialog>
```

**Логика:**
- текстовые фильтры — регистронезависимые `ILIKE`, множество значений через `OR`;
- числовые — `>=`, `<=`; если оба не заданы, фильтр не применяется;
- диапазоны дат — `BETWEEN`; поддержка значений только «от» или только «до»;
- чекбоксы добавляют дополнительные условия (`cost > 0`, `revenue > 0`, `roi_pct > 0`);
- пресеты сохраняются в `localStorage` и серверном конфиге (позже — в БД).

### 2.3 Алерты и мониторинг

**Типы алертов:**
- ROI < 0% (critical)
- ROI между 0 и 10% (warning)
- Spend > $X при 0 `tw_first_purchases` (warning)
- Установки > 0, `tw_registrations = 0` (warning)
- Отсутствие данных по кампании > N дней (info)
- Ошибки валидации при загрузке (error)

**Конфигурация:**
- файл `config/alerts.yml` хранит пороги (например, `roi_negative: 0`, `roi_low: 10`, `spend_no_conv_hours: 12`);
- таблица `alerts` (`id`, `type`, `severity`, `campaign`, `offer`, `detected_at`, `resolved_at`, `payload JSONB`);
- UI раздел «Алерты»: список с фильтрами по уровню и кампании, возможность пометить как решённый.

**Уведомления:**
- в MVP — логирование и отображение в UI;
- опционально — Slack/Telegram вебхук для критических предупреждений.

## Этап 3: Дополнительные функции (1-2 недели)

### 3.1 Базовые отчёты и аналитика

- Переключатель режимов: «Таблица», «Группировка по кампаниям», «Группировка по офферам», «Временная матрица».
- Группировка — пересчёт метрик по правилам раздела 2.1.
- Отображение трендов (линейные графики) по выбранным KPI (ROI, profit, spend).
- Возможность выгрузить текущую выборку в JSON (для интеграции с BI).

### 3.2 Улучшения UX

- Цветовая индикация: ROI > 0 зеленый, ROI < 0 красный, предупреждения — желтый.
- Прогресс-бары для `install2reg`, `reg2dep`.
- Настройка видимости колонок + сохранение в профиле пользователя (пока локально).
- Горячие клавиши (например, `Ctrl+F` открывает фильтр).
- Документация внутри приложения (tooltip с описанием метрик и формул).

## Технологический стек (MVP)

### Backend
- **Python 3.11+**
- **FastAPI** — REST API
- **PostgreSQL** — основная БД
- **SQLAlchemy** — ORM
- **Pydantic** — валидация входных данных
- **Pandas** — обработка CSV и расчёт метрик
- **Alembic** — миграции БД
- **APScheduler** (или Celery beat) — планирование фоновых задач
- **Great Expectations** (опционально) — автоматические тесты качества данных

### Frontend
- **HTML/CSS/JavaScript** (Vanilla)
- **Bootstrap 5** — базовая стилизация
- **Tabulator** или **AG Grid** — таблица и группировки
- **Chart.js** (опционально) — графики трендов
- **FilePond** (опционально) — удобная загрузка файлов

### Инфраструктура
- **Docker / Docker Compose** — локальный и dev-стек
- **Makefile** или `task` — команды (`make db-up`, `make etl-tw`, `make etl-moloco`)
- **Logging**: структурированные логи (JSON) для ETL и API
- **.env** файлы для конфигураций (пути `data/raw`, пороги алертов, валюта по умолчанию)

## Критерии готовности MVP

1. ✅ Поддерживается загрузка CSV через CLI, файлы сохраняются в `raw` и фиксируются в `data_loads`.
2. ✅ Работает pipeline валидации и нормализации (`tw_events`, `moloco_events`) с журналированием ошибок.
3. ✅ Витрина `campaign_statistics` содержит все поля из обоих источников с префиксами (tw_, m_) и расчётными колонками.
4. ✅ Метрики рассчитываются по единым формулам и корректно агрегируются при группировках.
5. ✅ UI отображает две таблицы (плоская + иерархическая) с возможностью скрытия колонок.
6. ✅ Фильтр открывается одной кнопкой, поддерживает `contains/not contains`, числовые диапазоны (больше/меньше), фильтр по гео.
7. ✅ Фильтры и отображаемые колонки работают независимо - можно фильтровать по скрытым колонкам.
8. ✅ Иерархическая таблица с кампаниями и вложенными офферами (разворачивание по клику).
9. ✅ Лог-записи по каждой загрузке хранятся в `data_loads`.
10. ✅ Документация по структуре данных и формулы метрик в `docs/`.

### Отложенные функции (см. docs/features_plan.md Этап 0)
- Инкрементальная загрузка (замена полного удаления на upsert)
- История загрузок в UI
- Пресеты фильтров (сохранение/загрузка)
- Экспорт данных (CSV/JSON/Excel)
- Индикация загрузки файлов в UI
- Unit и Integration тесты
- Обработка ошибок в UI (toast-уведомления)

## Таймлайн

- **Этап 1** → 1-2 недели (БД, ETL, API, загрузки)
- **Этап 2** → 2-3 недели (UI медиабаинга, фильтры, алерты)
- **Этап 3** → 1-2 недели (доп. отчеты, улучшения UX)
- **Итого** → ~4-7 недель

## Результат MVP

В распоряжении команды медиабаинга — единая таблица всех метрик по кампаниям и офферам с полнотой данных, историей загрузок и прозрачными формулами. Система позволяет вручную обновлять CSV, отслеживать качество, фильтровать и анализировать кампании, видеть базовые алерты и готова к расширению функциональности (офферы, финансы, автоматизация).

