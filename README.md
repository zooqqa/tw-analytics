# TW Buy Stat - Сквозная аналитика

Объединение данных TrafficWave и рекламных источников для анализа эффективности кампаний.

## 🚀 Быстрый старт

### Автоматическая установка (Windows)
```bash
scripts\setup.bat
scripts\load_data.bat    # поместите tw.csv и moloco.csv в корень
scripts\run_dev.bat
```

### Ручная установка
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
.venv\Scripts\activate     # Windows

pip install -r requirements.txt
python -c "from src.db.setup import ensure_schema; ensure_schema()"
python -m src.main --tw tw.csv --moloco moloco.csv
uvicorn src.api.main:app --host 0.0.0.0 --port 8000 --reload
```

🌐 **Откройте**: http://127.0.0.1:8000/

## ✨ Возможности

**Два режима таблиц**: иерархическая (кампании → офферы) и плоская
**Фильтрация**: текстовые (содержит/не содержит), числовые (больше/меньше), даты, гео
**Данные**: все поля TW/Moloco с префиксами + расчетные метрики (profit, ROI, CPI)
**Независимость**: фильтр по скрытым колонкам

## 📝 API

- `GET /` - интерфейс
- `GET /table` - плоская таблица
- `GET /table/hierarchical` - иерархическая
- `GET /stats` - статистика
- `GET /loads` - история загрузок

## 🛠 Стек

**Backend**: Python 3.11+, FastAPI, SQLAlchemy, Pandas
**БД**: SQLite (dev) / PostgreSQL (prod)
**Frontend**: Vanilla JS, HTML, CSS

## 📚 Документация

- `docs/PROJECT.md` - полное описание проекта
- `docs/features_plan.md` - планы развития
- `docs/idea.md` - первоначальная идея

## 🎯 Статус MVP: ✅ ЗАВЕРШЕН

Все критерии готовности выполнены. Следующий этап: `docs/features_plan.md`

