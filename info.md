## ТЕКУЩИЙ СТАТУС ПРОЕКТА (Октябрь 2024)

### ✅ Реализовано в MVP:

1. **Загрузка и обработка данных:**
   - CLI команда для загрузки CSV: `python -m src.main --tw tw.csv --moloco moloco.csv`
   - Валидация и нормализация данных из обоих источников
   - Хранение в PostgreSQL с полной историей загрузок
   - Все поля из обоих источников сохраняются с префиксами `tw_` и `m_`

2. **Витрина данных (`campaign_statistics`):**
   - Объединение данных TW и Moloco по ключу (дата, кампания)
   - Расчётные поля: `profit`, `roi_pct`, `calculated_spend`, `calculated_cpi`
   - Корректная агрегация процентных метрик при группировках

3. **UI (FastAPI + HTML/JS):**
   - **Два режима таблицы:**
     - Плоская таблица (все строки)
     - Иерархическая таблица (кампании с вложенными офферами)
   - **Фильтрация (одна кнопка с модальным окном):**
     - Текстовые фильтры: "содержит" / "не содержит"
     - Числовые фильтры: "больше" / "меньше" (ROI, Profit, Spend, Installs)
     - Диапазон дат
     - Фильтр по гео (содержит/не содержит)
   - **Независимость фильтров и колонок:** можно фильтровать по скрытым колонкам
   - **Сортировка:** по любой колонке в обоих режимах
   - **Выбор видимых колонок:** чекбоксы в модальном окне фильтров
   - **Агрегаты:** карточки с общей статистикой

4. **API endpoints:**
   - `GET /` - главная страница
   - `GET /table` - плоская таблица с данными
   - `GET /table/hierarchical` - иерархическая структура (кампании → офферы)
   - `GET /stats` - агрегированная статистика
   - `GET /loads` - история загрузок

5. **Технологический стек:**
   - Backend: Python 3.11+, FastAPI, SQLAlchemy, Pandas, PostgreSQL
   - Frontend: Vanilla JS, HTML, CSS (dark theme)
   - База данных: PostgreSQL
   - Зависимости: установлены глобально

### 📋 Как запустить:

```bash
# 1. Запустить сервер (из корня проекта)
uvicorn src.api.main:app --host 0.0.0.0 --port 8000

# 2. Загрузить данные (если нужно обновить)
python -m src.main --tw tw.csv --moloco moloco.csv

# 3. Открыть в браузере
http://127.0.0.1:8000/
```

### 📁 Структура проекта:

```
tw_buy_stat/
├── src/
│   ├── api/
│   │   └── main.py          # FastAPI приложение и эндпоинты
│   ├── db/
│   │   ├── models.py        # SQLAlchemy модели
│   │   └── session.py       # Подключение к БД
│   ├── ingestion/
│   │   ├── moloco_loader.py # Загрузка Moloco CSV
│   │   └── tw_loader.py     # Загрузка TW CSV
│   ├── services/
│   │   └── merge_service.py # Мерж данных в campaign_statistics
│   └── main.py              # CLI для загрузки данных
├── templates/
│   └── index.html           # UI (single page app)
├── docs/                    # Документация
├── tw.csv                   # Пример данных TW
├── moloco.csv              # Пример данных Moloco
└── requirements.txt         # Зависимости Python
```

### 🔄 Следующие шаги (см. docs/features_plan.md):
- Инкрементальная загрузка вместо полного удаления
- UI для истории загрузок
- Пресеты фильтров
- Экспорт данных (CSV/Excel)
- Unit и Integration тесты
- Обработка ошибок в UI

---

## ОРИГИНАЛЬНЫЙ ЗАПРОС:

Запрос:
Ты профессиональный архитектор ИТ систем, эксперт в бизнес аналитике и построении CRM, ERP.
Идея сервиса - сквозная аналитика между треккером и рекламным источником. Возможность отслеживать показатели рекламных кампаний, креативов, офферов и т.п. 
А так же возможность отслеживать взаимоотношения с партнерскими программами, которые выдают эти офферы. Чтобы прогнозировать рекламный бюджет на неделю. Чтобы отслеживать прибыль нашей компании по направлению работы с трафиком, при чем видеть суммы долга партнерских программ ориентировочные даты выплат, а так же видеть фактическую прибыль и баланс.

У нас есть треккер и источник (на данный момент moloco, дальше будет facebook, google и т.п. принципы везде одинаковые). Треккер перенаправляет клики с рекламы на разные ссылки (офферы) и отслеживает показатели этих офферов. Из офферов приходят постбеки в треккер на события (регистрация, депозит, сумма депозита, повторный депозит). Соответственно в треккере формируется аналитика по показателям офферов. В треккер так же попадает информация о названии рекламной кампании и в будущем о названии креативов. С рекламной кампании трафик попадает на различные офферы, мы их меняем постоянно, пытаясь найти наилучший, так же на них могут заканчиваться капы (обьем депозитов) или трафик может не пройти заданный KPI в определенный срок, эти моменты нужно отслеживать и переключать офферы при необходимости.

Мы хотим взять две базы (на первом этапе просто 2 csv файла базы данных аналитики. второй этап это тянуть данные по api) из сервиса - треккера taffic wave и из рекламного источника moloco. И обогатить, смержить эти две базы. Взяв необходимое из разных мест, так как в молоке есть данные о затратах, а в треккере есть данные о прибыли.
Рекламная кампания запускается в молоке, треккер получает название рекламной кампании и в будущем будет получать название креативов (чтобы отслеживать их эффективность).
Из рекламной кампании в треккере идет распределение по ссылкам (офферам), у оффера есть свои параметры и разные показатели по прибыли. Мы хотим отслеживать эти показатели и при необходимости переключать офферы или останавливать рекламные кампании, если они идут в минус.

Сам оффер состоит из названия продукта, названия партнерской программы (или владельца продукта), преленд (регистрация, определенная страница на продукте), подход (slots, crush), страна, ставка (сумма выплаты партнеру\CPA), кап (количество депозитов), средний чек, количество и процент повторных депозитов, сумма депозитов, процент от достижения KPI (различные формулы, например соотношение повторных депозитов к первым депозитам - 60% или сумма депозитов больше или равна 70%). А так же партнерская программа или владелец продукта выдает разные временные промежутки по которым отслеживает показатели офферов и переодически называет нам даты, когда он выплатит нам нашу прибль. Прибыль - это количество CPA событий перемноженное на ставку и с зависимостью от KPI (на одних офферах он есть, на других нет).

Я бы разделил эту задачу на несколько составляющих логически и визуально на сервисе, может быть это должно быть несколько разных сервисов, которые взаимодействуют между собой.

1) Работа медиабаинга. Это аналитика рекламных кампаний. Нужно видеть показатели конверсии кампааний и вложенных в них офферов, чтобы переключать офферы, улучшать работу кампаний.

2) Работа affiliate менеджера - это работа с офферами и продуктами, внесение всех данных об оффере (ставка, KPI, капа, название продукта, партнерки и т.п.), а так же отслеживание остатков кап, наличия офферов под нужные гео (страны). Отслеживание дат и сумм выплат от партнерских программ и за какой период (или количество депозитов) происходят выплаты.

3) Показатели для собственника или финансового директора по прибыльности.

В идеале здесь должны быть такие алгоритмы, чтобы как можно меньше работников требовалось, для отслеживания и поддержания этих сервисов (не включая тех спецов, которые этот сервис делают). Например можно подсвечивать или уведомлять о том, что заканчиваются капы, что kpi скорее всего не пройдем. Помощь медиабаингу - что в тот или иной оффер прибыльнее\лучше конвертит, что определенная кампания идет с хорошим плюсом, а другая кампания подбирается к нулю или минусовому показателю.

Сервисом будет пользоваться несколько медиабаеров и им нужно видеть соответствующие показатели, будет пользоваться аффилейт менеджер занимающийся офферами, будет пользоваться финансист или собственник, чтобы видеть фин показатели. На данный момент собственник является и финансистом и аффилейтом и немного медиабаером. Но для того чтобы масштабироваться, нужен сервис, который позволит нанимать и разграничивать обязанности.
Собственно вопрос как достичь такого сервиса, как его реализовать, какие тут могут быть этапы, целесообразно ли сюда внедрять ИИ или можно сделать все кодом и алгоритмами.

Ответ:
Ниже — целостный план, как спроектировать и поэтапно построить сервис сквозной аналитики и операционного контроля между трекером и рекламными источниками с учётом офферов, партнёрок, KPI, капов и финансов.

1) Цели и ключевые принципы
- Оперативная аналитика медиабаинга: связать траты из источника (Moloco → далее FB/Google) и доход/ивенты из трекера, видеть эффективность кампаний/креативов/офферов.
- Управление офферами и отношениями с партнёрскими программами: капы, KPI, окна оценки, графики и суммы выплат, наличие офферов по нужным GEO.
- Финмодуль: прибыль/убыток, задолженности партнёрок (AR), фактические выплаты, баланс и еженедельный прогноз бюджета/кэша.
- Автоматизация: уведомления, рекомендации и минимизация ручного контроля; в перспективе частичная автопауза/переключение.

Архитектурные принципы:
- Каноническая модель данных и унификация идентификаторов по источникам.
- Отделение OLTP (операционные справочники, статусы, правила) и аналитики (витрины/мартов) с расчётным движком.
- Идемпотентные загрузки, обработка поздних событий, нормализация часовых поясов и валют.
- Версионирование офферов/политик выплат, аудит изменений.
- Роль‑бэйзд доступ (RBAC), прозрачные объяснения расчётов.

2) Каноническая модель данных (ядро)
Сущности (ключевые поля):
- `AdSource` (Moloco/Facebook/Google), `Campaign`, `AdGroup`, `Creative` — внешние ID, имена, валюта/таймзона.
- `TrackerClick` — `click_id`, `campaign_external_id`, `adgroup_external_id`, `creative_external_id`, `offer_id`, `geo`, `ts`.
- `ConversionEvent` — `click_id`, `event_type` (registration, first_deposit, repeat_deposit), `amount`, `ts`, `offer_id`.
- `Offer` — продукт, партнёрская программа, GEO, ставка (`rate`, валюта), кап (по типу события — чаще FTD/депозит), преленд, подход, статус.
- `AffiliateProgram` — платёжные условия и расписание выплат: `payment_terms` (NET7/NET14), `payment_schedule` (weekly Tue, monthly 15th), `hold`, `currency`.
- `KpiPolicy` — формулы и окна оценок: тип KPI (например, ratio `repeat_deposit / first_deposit ≥ 0.6` за 7/14 дней; или `sum(deposit) ≥ X`), `window_days`, зависимости от GEO/продукта.
- `PayoutPolicy` — как KPI влияет на оплату: бинарно (pay/no-pay) или шкала (0/50/100%), связь с `KpiPolicy`.
- `CapPolicy` — лимиты по офферу: `type` (FTD/депозиты/оборот), период (суточный/неделя/месяц), приоритет.
- `Spend` — фактические траты из источника: по `campaign/adgroup/creative` на дату/час, валюта.
- `RevenueEvent` — производная от `ConversionEvent` + применённые `PayoutPolicy`/KPI/окно; состояния: pending → qualified → invoiced → paid → rejected/adjusted.
- `Receivable` — задолженность партнёрки: сумма, период, ETA выплаты, статус и фактическая дата.
- `Budget` — бюджеты кампаний/источников и прогнозы.
- `User`, `Role`, `Alert`, `DecisionLog` — пользователи, права и журнал решений/автодействий.

Важно: для всех сущностей — `valid_from/valid_to` (SCD2) при изменениях ставок/KPI/кап.

3) Интеграция и сшивка данных
- Обязательная унификация идентификаторов на уровне клика и кампаний:
  - В трек‑URL из источника прокинуть макросы `campaign_id`, `adgroup_id`, `creative_id`, `placement`, `country`, и уникальный `click_id` (генерируете в трекере). Тот же `click_id` возвращается в постбэках.
  - В трекере сохранять внешние ID, а не только имена кампаний. Это критично для корректного джоина затрат и дохода.
- Таймзона: нормализовать события в UTC, хранить исходную TZ для отчётов «как в интерфейсе источника».
- Валюта: хранить исходную и пересчитанную в базовую (например, USD) по курсу на дату события/инвойса.
- Идемпотентные загрузки: upsert по уникальным ключам (`external_id + date` для spend, `event_id` или `click_id+type+ts` для конверсий).
- Обработка «поздних событий»: пересчитывать витрины в «скользящем окне» (например, последние 14–30 дней).

CSV на MVP:
- Из трекера: поля минимум — `click_id`, `campaign_external_id`, `adgroup_external_id`, `creative_external_id`, `offer_id`, `geo`, `event_type`, `event_ts`, `amount`.
- Из Moloco: `date`, `campaign_external_id`, `adgroup_external_id`, `creative_external_id`, `impressions`, `clicks`, `spend`, `currency`.

4) Расчётная логика
- Прибыль/ROI:
  - Profit = ApprovedPayout − AdSpend.
  - ROAS = ApprovedPayout / AdSpend; ROI% = Profit / AdSpend.
- KPI/окна:
  - Для каждого `ConversionEvent` определяем принадлежность окну (например, repeat_deposit в течение 7 дней от FTD).
  - Агрегируем метрики в окне на уровне `offer` (и, при необходимости, `campaign/geo`), применяем `KpiPolicy` и получаем коэффициент оплаты.
- Payout:
  - `qualified_count = floor(kpi_factor * cpa_events_in_window)` либо бинарное 0/1 применить к ставке.
  - `RevenueEvent` фиксирует сумму и статус (pending/qualified/invoiced/paid).
- Капы:
  - `remaining_cap = cap_limit − qualified_count` по периоду (суточный/недельный).
  - Состояния: warn ≥ 80%, stop ≥ 100% (или заранее с учётом темпа).
- Задолженность и выплаты:
  - Генерация `Receivable` по условиям `AffiliateProgram`: период, сумма, ETA с учётом hold и `payment_terms`.
  - Аджастменты/чарджбеки — корректирующими `RevenueEvent` с ссылкой на исходные события.
- Прогноз бюджета/кэша (неделя):
  - Spend: ETS/Prophet/LightGBM на ряду трат по `campaign/adgroup` с учётом бюджета/пейсинга.
  - Revenue/платежи: прогноз квалификации KPI + календарь выплат партнёрок → кэш‑ин. Кэш‑аут — график платежей источникам. Сводим в прогноз баланса.

5) Модули/экраны по ролям
- Медиабаинг:
  - Дашборд кампаний/адсетов/креативов: CTR/CR/FTD/ARPU/ROAS/ROI, сплит по офферам, кап‑остатки, тренды.
  - Карта связей кампания → офферы с вкладом каждого; подсветка аномалий.
  - Рекомендации: какие офферы скейлить/выключить; где KPI не успевает набраться в окне; где кап «горит».
- Affiliate‑менеджер:
  - Реестр офферов: ставка, GEO, KPI/окно, капы, доступность, истории изменений.
  - Партнёрские программы: условия выплат, ETA, задолженность по периодам, инвойсы.
  - Наличие офферов под нужные GEO/подходы, алерты по выработке/приостановке.
- Финансы/собственник:
  - P&L по срезам (источник/кампания/оффер/GEO/период).
  - AR‑aging по партнёркам, графики фактических выплат, прогноз кэша.
  - План/факт бюджета, сценарный анализ.

6) Автоматизация: правила → эвристики → ML
Стартуйте с правил, затем добавляйте прогнозы:
- Правила (DSL/конфиг):
  - Кап: `cap_fill_rate >= 0.8` → warn; `ETA_to_cap_exhaustion < 6h` → critical.
  - KPI‑риск: `projected_kpi_prob < 0.6` при прошло ≥ X% окна → warn.
  - ROI‑контроль: `ROI_rolling_3d < threshold` и `spend_today > min` → suggest pause/offer switch.
- Эвристики:
  - Авто‑аллокатор трафика между офферами на базе epsilon‑greedy/Thompson Sampling с ограничениями по капу/KPI.
- ML (этап 3+):
  - Классификатор вероятности FTD и повторного депозита по фичам клика/креатива/оффера/GEO/часа.
  - Прогноз time‑to‑deposit (survival analysis) для оценки успеваемости окна KPI.
  - Прогноз трат/пейсинга и ожидаемого ARPU для ROAS‑ориентированных рекомендаций.
- LLM имеет смысл для: автогенерации объяснений/сводок, роутинга алертов и интерактивных запросов на естественном языке. Числовые решения — на классических моделях/правилах.

Пример правила в YAML (MVP):
```yaml
rules:
  - id: cap_warn
    when: cap_fill_rate >= 0.8
    scope: [offer_id, period=day]
    action: notify(channel="telegram", severity="warning")

  - id: kpi_risk
    when: kpi_projection < 0.6 and window_progress >= 0.5
    scope: [offer_id, campaign_id]
    action: suggest("reduce_traffic", reason="KPI likely to fail")

  - id: roi_stop
    when: roi_3d < -0.15 and spend_today > 200
    scope: [campaign_id]
    action: suggest("pause_campaign")
```

7) Этапы внедрения
Этап 0 — Домен/ID‑стратегия:
- Утвердить канонические поля, макросы в трек‑URL, единые названия измерений, часовой пояс, базовую валюту.
- Определить минимальные схемы CSV и валидации.

Этап 1 — MVP на CSV:
- Хранилище: Postgres для OLTP + витрины (можно в том же PG/Timescale).
- Загрузки CSV в `staging_*` с валидацией, трансформы в `core_*` (dbt/Dagster).
- Базовые витрины: `f_spend_daily`, `f_conversions`, `f_payouts`, `f_profit`.
- Простейшие алерты (почта/Telegram), дашборды (Metabase/Superset).
- UI: реестр офферов и партнёрок, ввод KPI/кап/условий выплат, просмотр задолженности.

Этап 2 — API‑коннекторы:
- Инкрементальные коннекторы Moloco и трекера (FastAPI/Dagster/Airflow), обработка ретро‑правок.
- Креативы и adgroup — детализировать разрезы.
- Календарь выплат, генерация `Receivable` и статус‑трекинг.

Этап 3 — Правила/автодействия:
- Rules‑engine, журнал предложений и автоприменение (по флагу).
- Интеграция с трекером/источником по API для автопаузы/переключения офферов (где допустимо).
- Прогноз бюджета/кэша на неделю с доверительными интервалами.

Этап 4 — ML/оптимизация:
- Модели вероятности FTD/повторного депозита и time‑to‑event.
- Мультиармный бандит для распределения трафика между 2–5 офферами в кампании.
- A/B‑телеметрия, стабилизация.

8) Технологии (реалистично и быстро)
- Бэкенд/API: `FastAPI` (Python) или `NestJS` (TypeScript). Планировщик: `Dagster` или `Airflow`.
- БД: `PostgreSQL` (+ `TimescaleDB` для таймсерий) для MVP. Далее — DWH (BigQuery/Snowflake) при росте.
- Трансформации: `dbt`.
- Дашборды: `Metabase`/`Apache Superset`.
- Авторизация/RBAC: `Keycloak` или `Auth0`.
- Уведомления: Telegram/Slack вебхуки.
- ML: `scikit-learn`, `lightgbm`, `prophet` (на старте достаточно).
- Логи/мониторинг: `Grafana` + `Prometheus`, алертинг по SLO пайплайнов.

9) Нюансы и риски
- Несоответствие идентификаторов: без внешних ID из источника в трекере корректный мэппинг затруднён (решить через макросы).
- Валюты и курсы: зафиксировать политику пересчёта (по дате события vs по дате инвойса).
- Поздние конверсии/аджастменты: сделать механизм перерасчёта «скользящего окна».
- Версионирование офферов/правил: хранить `valid_from/valid_to`, аудит изменений и переигровка истории при изменении политики — только проспективно.
- GDPR/PII: хранить минимум персональных данных (желательно только агрегаты/анонимные идентификаторы).

10) Минимальные схемы CSV (пример)
Трекер (`tracker_events.csv`):
- `click_id` (string)
- `ts` (ISO8601 UTC)
- `campaign_external_id`, `adgroup_external_id`, `creative_external_id` (string)
- `offer_id` (string)
- `geo` (string, ISO2)
- `event_type` (registration|first_deposit|repeat_deposit)
- `amount` (float, для депозитов)
- `currency` (string)

Moloco (`moloco_spend.csv`):
- `date` (YYYY-MM-DD)
- `campaign_external_id`, `adgroup_external_id`, `creative_external_id`
- `impressions`, `clicks`
- `spend` (float)
- `currency` (string)
- `timezone` (string, опционально)

11) Что можно сделать «ИИ vs правила»
- 80% пользы даст «чистая» инженерия: модель данных, корректная сшивка, окна KPI, капы, алерты и простые прогнозы/эвристики.
- ML целесообразен, когда наберёте историю кликов/ивентов 50k+ на сегмент: уменьшит время реакции и снизит «ложные паузы».
- LLM применять точечно: объяснения, генерация кратких дневных сводок, чат‑интерфейс к метрикам.

Следующие шаги
- Утвердить канонический набор полей и макросы в трек‑URL для Moloco; проверить, что трекер пишет внешние ID.
- Согласовать схемы CSV и прислать образцы по 2–4 неделям для прототипа.
- Развернуть Postgres + Metabase, собрать первые витрины и дашборды за 3–5 дней.
- Завести `Offer`, `AffiliateProgram`, `KpiPolicy`, `PayoutPolicy`, `CapPolicy` в OLTP и связать с расчётами.
- Подключить правила алертов и Telegram‑нотификации.
- Спланировать API‑коннекторы и переход на инкрементальные загрузки.

Если хотите, подготовлю:
- ER‑схему с полями и ключами для `core_*` и `mart_*`.
- Прототип `dbt`‑моделей для KPI‑окон и payout‑квалификации.
- Спецификацию коннектора Moloco/трекер и шаблоны трек‑URL с макросами.



ответ от другой ИИ:
Анализ и рекомендации по реализации сервиса сквозной аналитики
Архитектурный подход
Ваша задача действительно требует создания комплексной системы из нескольких взаимосвязанных модулей, а не единого монолитного сервиса. Это правильная интуиция о разделении на составляющие.
Рекомендуемая структура системы
1. Модуль Медиабаинга (Media Buying Dashboard)
Целевая аудитория: Медиабайеры
Ключевой функционал:

Реал-тайм мониторинг конверсий рекламных кампаний
Сравнительная аналитика офферов внутри кампаний
Автоматические алерты о негативных трендах (приближение к нулю/минусу)
Рекомендательная система по переключению офферов
Визуализация воронки: кампания → креатив → оффер → конверсия

2. Модуль Affiliate-менеджмента
Целевая аудитория: Affiliate-менеджер
Ключевой функционал:

База офферов с полной спецификацией (ставка, KPI, капы, продукт, партнёрка)
Мониторинг остатков капов с предупреждениями
Матрица доступности офферов по гео
Календарь выплат от партнёрских программ
Трекинг долгов партнёрок с прогнозом поступлений
Расчёт фактической прибыли с учётом KPI

3. Финансовый модуль (Finance Dashboard)
Целевая аудитория: Собственник/CFO
Ключевой функционал:

Сводная прибыльность в разрезе: кампании, гео, партнёрки, периоды
Прогноз недельного бюджета на рекламу
Баланс компании (затраты vs ожидаемые/фактические выплаты)
Дебиторка от партнёрок с датами закрытия
ROI и payback period по направлениям

4. ETL/Интеграционный слой

Сбор данных из Traffic Wave и Moloco (CSV → API)
Нормализация и обогащение данных
Мерж данных о затратах (Moloco) и прибыли (Traffic Wave)
Хранилище агрегированных данных

Этапы реализации
Этап 1: MVP (2-3 месяца)

Ручная загрузка CSV из обеих систем
Простой скрипт слияния данных
Базовые дашборды в BI-инструменте (Power BI/Tableau/Metabase)
Основные метрики для медиабаинга
Простейшая система алертов (email при критических событиях)

Этап 2: Автоматизация (2-3 месяца)

API-интеграция с Traffic Wave и Moloco
Автоматическое обновление данных (раз в час/день)
Разработка веб-интерфейса с ролевой моделью
Модуль управления офферами для affiliate-менеджера
Прогнозирование капов и KPI

Этап 3: Интеллектуализация (3-4 месяца)

Рекомендательные алгоритмы для переключения офферов
Прогнозирование недельного бюджета на основе трендов
Автоматическое определение проблемных кампаний
Система приоритезации действий для медиабайеров

Этап 4: Масштабирование

Добавление Facebook, Google Ads
Расширенная аналитика креативов
A/B тестирование офферов
Автоматизация ротации офферов по правилам

Вопрос о применении ИИ
Где ИИ целесообразен:

Прогнозирование

Предсказание вероятности достижения KPI офферами
Прогноз недельного бюджета с учётом сезонности
Оценка остаточного срока жизни капов


Рекомендательные системы

Подбор оптимальных офферов для кампаний
Ранжирование приоритетов действий для медиабайеров
Кластеризация успешных паттернов (гео+продукт+креатив)


Детекция аномалий

Выявление нетипичных падений конверсий
Раннее обнаружение фрода или технических проблем



Где достаточно алгоритмов:

Расчёты и агрегации

Подсчёт прибыли, ROI, конверсий
Мониторинг остатков капов
Расчёт дебиторки


Базовые алерты

Превышение порогов (минус по кампании, 90% капа)
Напоминания о датах выплат


Стандартная визуализация

Графики, таблицы, дашборды



Технологический стек (рекомендации)
Backend:

Python (ETL, аналитика, ML если потребуется)
PostgreSQL (основное хранилище)
Redis (кеширование, очереди задач)

Frontend:

React/Vue для кастомных интерфейсов
Или готовое BI-решение (Metabase, Redash) для быстрого старта

Оркестрация:

Airflow для планирования ETL-задач

Критически важные моменты

Ролевая модель с первого дня - медиабайеры не должны видеть финансы, но должны видеть свои кампании
Аудит изменений - кто, когда и что менял в настройках офферов
Историчность данных - хранить срезы параметров офферов (ставка могла измениться)
Гибкость формул KPI - должна быть возможность конфигурировать без изменения кода
Масштабируемость интеграций - архитектура должна легко принимать новые источники (Facebook, Google)

Вывод
Ваш сервис — это специализированная CRM для арбитража трафика с элементами BI и финансового учёта.
ИИ на старте не обязателен — можно начать с умных алгоритмов и правил. ИИ стоит внедрять на этапе 3-4, когда накопится достаточно исторических данных для обучения моделей и станет понятно, где автоматизация даст наибольший эффект.
Ключ к успеху — итеративная разработка с постоянной обратной связью от медиабайеров и affiliate-менеджера, начиная с простого MVP.